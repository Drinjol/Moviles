<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="CommunyStoreFrontEnd.EditarPerfil"
             Title="EditarPerfil">
    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="10">
            <Label Text="Editar Perfil" FontSize="Large" HorizontalOptions="Center" />

            <Label Text="Nombre de Usuario" />
            <Entry Text="{Binding NombreUsuario}" Placeholder="Nombre de Usuario" />

            <Label Text="Descripción" />
            <Entry Text="{Binding Descripcion}" Placeholder="Descripción" />

            <Label Text="Dirección" />
            <Entry Text="{Binding Direccion}" Placeholder="Dirección" />

            <Label Text="Teléfono" />
            <Entry Text="{Binding Telefono}" Placeholder="Teléfono" />

            <Label Text="Email" />
            <Entry Text="{Binding EmailUsuario}" Placeholder="Email" />

            <Button Text="Guardar Información" Clicked="GuardarInformacion_Clicked" />
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
